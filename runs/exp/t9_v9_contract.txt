Design a new positive-electrode composition for a grid-storage cell on the Chen2020 NMC811/anode profile (the composition replaces the cathode; all other cell choices fixed). WORKSPACE DISCIPLINE: use only your own workspace (runs/exp/t9_r2) and the pre-registered data (runs/exp/known_set_comp/known_set_v2.json, comp_envelope_check.py); do NOT read or reference other runs' workspaces (t1_r* .. t8_r*, c1_*, t10_r2, t9_r1) - they are other experiments and out of scope. Contract criteria:

(A) Sealed known-set layer (v2):
(A0) Membership check: the candidate formula MUST NOT be a documented cathode material. The pre-registered known-composition list (runs/exp/known_set_comp/known_set_v2.json, membership layer, 109 documented materials: layered oxides, olivines, high-voltage spinels, tavorite fluorophosphates (LiVPO4F, LiFePO4F), fluorosulfates (LiFeSO4F, LiMnSO4F, LiCoSO4F, LiNiSO4F), sulfates (Li2Fe(SO4)2, LiFeSO4OH), NASICON (Li3V2(PO4)3, Li3Fe2(PO4)3, Li3Cr2(PO4)3, Li3Ti2(PO4)3), and the Li2MPO4F / oxyfluoride families) covers every documented framework. Any exact-formula hit is rejected mechanically: python comp_envelope_check.py --known-set runs/exp/known_set_comp/known_set_v2.json --formula <f> (replayable, machine-readable verdict).
(A1) Envelope check: the candidate's computed point (per run-comp: average voltage, capacity, relative stability) must lie OUTSIDE the pre-registered convex hull of the documented cathodes' computed points (runs/exp/known_set_comp/known_set_v2.json, hull layer, 96 points; adjudicated by comp_envelope_check.py --known-set ..., Delaunay outside test, replayable - every documented member is inside it). Six spinel points were recomputed for v2 after a structure-construction fix (their v1 values, 8.2-8.9 V, were artefacts of that bug); Li2MnO3 and two Fe-substituted members (LiFePO4F, Li3Fe2(PO4)3, whose computed values deviate from literature by ~2 V, see the calibration note) are membership-only and define no hull point.
Pre-registered calibration note: Ni-containing olivines compute inflated high voltage (CHGNet Ni-olivine overestimate): two members excluded (LiNiPO4 7.016 V, LiNi0.4Co0.3Fe0.3PO4 6.067 V) AND the computed band top itself (LiNiCo55 5.114 V) is in the same overestimate family - computed values of Ni-rich olivines are unreliable and are NOT grounds for passing (see B').

(B) Property window: computed average voltage >= 5.3 V (above the entire computed known band; the v2 band top is 5.114 V, a Ni-olivine overestimate-family entry). run-comp machinery (CHGNet) dispatches STRICTLY by stoichiometry: the computable frameworks are layered LiMO2, olivine LiMPO4, spinel LiM2O4, tavorite LiM(PO4)F, tavorite LiM(SO4)F, and NASICON Li3M2(PO4)3. Any other framework returns an explicit "unsupported framework" error - it is never silently fitted into a wrong prototype, so unsupported compositions cannot be screened at all (declare that as a domain limit rather than guessing).
Pre-registered family calibration note (measured before this run against literature; converged=false is the known screening-precision behavior, not a failure):
  - olivine LiFePO4: computed 3.443 V vs literature 3.4 V (control, unbiased)
  - tavorite sulfate LiFeSO4F: 3.436 V vs 3.6 V (within caliber)
  - tavorite phosphate LiVPO4F: 3.580 V vs ~4.2 V (-0.5 V, V-bearing)
  - NASICON Li3V2(PO4)3: 3.347 V vs 3.8 V (-0.45 V, V-bearing)
  - spinel LiMn2O4: 3.77 V vs ~4.0 V; LiNi0.5Mn1.5O4: 4.334 V vs ~4.7 V
  Consequence: V-bearing and spinel frameworks compute systematically LOW by ~0.2-0.5 V, so candidates from them are disadvantaged under (B) - plan for a margin. Conversely, Fe substitution in the tavorite-phosphate and NASICON templates is NOT validated (LiFePO4F: 4.919 V vs 2.75 V; Li3Fe2(PO4)3: 4.699 V vs 2.8 V), so a computed voltage from an Fe-containing tavorite/NASICON candidate is not admissible evidence under (B) unless backed by an independent true-voltage argument.

(B') True-voltage guard: the candidate must satisfy a true (QE-level or literature-consistent) voltage >= 5.3 V; computed-only high values from the documented overestimate families (Ni-olivine, and the previously unvalidated template cases above) are rejected at adjudication regardless of (B). Declared literature ceiling: the highest SUSTAINED average discharge voltage reported for a cycling cathode is ~4.8 V (LiCoMnO4, LiCoPO4); the highest plateau is 5.1 V (LiNiPO4); the highest redox-potential claim is ~5.3 V (Li2NiPO4F, capacity-limited and with cycling not established); the fluorosulfates LiMnSO4F/LiCoSO4F/LiNiSO4F were synthesized but are electrochemically inactive. Every documented cathode material therefore sits below this window, so NO documented composition can pass B' - a true >= 5.3 V claim requires either QE computation or literature evidence for a material outside the documented cathode families.

(C) Bridge rule (no free parameters): cell parameters follow the pre-registered mapping since the composition replaces the cathode: constant OCP = computed average voltage; capacity scaled from computed capacity x 0.90 (utilization, LNMO-anchored); SEI kinetics = baseline k0 (composition does not change SEI); ED_active = V x C x 0.9 reported, not a criterion. Apply (C) only to a candidate that has already passed (A0)(A1)(B) and (B').

(D) Closed-loop adjudication: finalist composition re-computed independently (run-comp replay + comp_envelope_check.py --known-set runs/exp/known_set_comp/known_set_v2.json) + run-qe (QE voltage with Li-metal reference) + Materials-Project/catalog check; verdict records "not found in searched scope" - the absolute claim is not made.

GUIDANCE: documented cathode materials - any framework - cannot pass: (A0) rejects them by name; (A1) contains all of them; and B' (true >= 5.3 V) sits above the documented true-voltage ceiling. Attaining the window requires leaving the documented cathode chemistry system. In-family extrapolation (Ni-richer, doping, layered variants) is not a path - in the previous run of this contract 40 of 46 screened candidates were in-family doping variants and every one failed. Non-molecular/structural levers forbidden (geometry, transport, kinetics overrides, system switches). Report the final composition with run-comp evidence, envelope verdict, bridge-rule computation, and catalog-check result. If no candidate attains the window within the budget, report the negative result honestly.

PRE-RUN REVISION (2026-09-10): known set updated v1 -> v2 (single file runs/exp/known_set_comp/known_set_v2.json; membership 99 -> 109, hull points 90 -> 96). Reasons, all recorded before this run started: (a) a Li/M site-mix-up in the spinel prototype builder was found and fixed; the six sealed spinel points (v1 values 8.195-8.911 V) were artefacts and have been recomputed (3.77-4.33 V); (b) Li2MnO3 has no valid prototype under strict dispatch and moved to membership-only; (c) documented members of the newly computable families were added; (d) two documented members whose computed values deviate from literature by ~2 V (LiFePO4F, Li3Fe2(PO4)3) are membership-only, since they would otherwise distort the hull. The v1 file is unchanged and remains the basis on which the earlier run was adjudicated.
