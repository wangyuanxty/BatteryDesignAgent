# Panel (d) — Outcomes: three decision-makers, three scores (abstract chart)

A minimal abstract bar/comparison chart, flat vector, white background.

Three columns of stylized "totem" figures, each column a different producer:
1. Left column (gold crown + ticks): "governed agent" — a tall column of eight stacked gold checkmarks, a small label "8/8" below.
2. Middle column (gear icon): "Bayesian optimizer" — column with only three gold checkmarks and five gray hollow circles, label "3/8".
3. Right column (twisted ruler icon): "protocol-free agent" — column with no fills, hollow circles only, label "0/8".

A thin horizontal baseline and a small legend row at the bottom. The three columns share the same height axis so the 8/8 vs 3/8 vs 0/8 contrast is instantly readable.

Style: flat, restrained, one gold accent + gray, no photo, no gradients, concise labels (max ~8 words total).
