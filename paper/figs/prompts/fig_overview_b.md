# Panel (b) — Core innovation: governance over the yardstick (abstract diagram)

A clean abstract diagram, flat vector style, white background, minimal palette (dark slate + one red accent + gold checkmarks).

Concept to show in ONE coherent diagram: a large rectangular "design loop" (circular arrows with stage chips: "electrolyte", "separator", "electrode", "architecture", "thermal") in the upper half. A small arm of the loop ends at a GOLDEN CONTRACT SCROLL (labeled "contract") sitting at the bottom-left. Between loop and contract runs a dashed "verdict corridor": a row of small verdict cards ("pass/fail") each stamped by a red seal icon.

Below the loop, a separate enclosed band labeled like a courtroom gallery, with three simple "judging machines": a scale icon, a magnifier over a code tag, and a list-check ticker — representing the three governance units. A thin arrow from the band says "adjudicates outside".

Key: the loop and the verdict band are SEPARATED by a visible "outside-agent" boundary line (thin gray divider), showing that judgment never comes from inside the loop.

Style: flat 2D, geometric, infographic-clean, enough detail at review size, no person, no photo, no gradients, NO more than 8 short text labels, all text English.
