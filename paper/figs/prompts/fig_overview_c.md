# Panel (c) — Architecture overview: one loop, five fidelity levels (diagram)

A flat schematic diagram, vector style, white background, using a vertical ladder metaphor.

Vertical spine with 5 rungs (top to bottom), each rung a pill labeled: "planning", "molecular screening", "cell design", "safety assessment", "true-compute endorsement". A small tag on the right of each pill states its cost/time in gray: "ms", "s", "min", "s", "hours". On the far left, a small golden keyhole icon at the top labeled "contract = sole input"; a vertical arrow runs the whole spine downward and loops around a small side loop with a "fallback?" diamond — only two diamonds, one loopback arrow, nothing else.

On the right side of the spine, a shaded vertical band labeled "adjudication & audit ledger" spanning all rungs, connected by thin icons per rung (page + stamp).

Style: flat, geometric, mono visual weight, one gold accent, remainder grays/slate, text labels minimal so they stay legible in a small panel, no photo.
