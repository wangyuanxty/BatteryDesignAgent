# 项目约定

## 写作与用词（硬性）

适用于本仓库的所有产出：论文（LaTeX）、幻灯、README、文档、代码注释、commit message，以及对话回复。

### 一、不自创术语，只用已经有的词

**这是本仓库最常被违反的一条。** 适用于**所有产出**：论文、文档、幻灯、代码注释、commit message，**以及对话回复和中间讨论稿**——讨论稿里造的词会顺着流进论文。

写下一个词之前先自问：**"这个词在论文 / 领域通用说法 / SKILL.md / 日志里出现过吗？"** 没出现过 → 换成人话描述，不要现起名字。

禁止：
- 自造复合名词（中文 X+Y 式："XX 契约 / XX 判据 / XX 闸"）
- 自造缩写、自造模式名、自造概念名
- 拿比喻当术语
- 把英文词硬译成中文当术语（default → "缺省"）

**唯一例外**：给架构起名字——因为"能命名"是架构的要求之一。但**必须先说明理由并得到用户同意**，不能先用了再说。

**本仓库已出现过的自造词（不要再造新的，已造的按右列改）**

| 禁用 | 改用 |
|---|---|
| 原语决策 | 每轮要先回答的问题 |
| 载波（约束-变量载波匹配） | 哪类变量能撼动这条约束 |
| 账 / 五本账 | 守恒式（质量守恒、电荷守恒…） |
| 缺省对照 | 对照做法 |
| 经验桥 / 桥 | 中间隔着一条拟合出来的经验公式 |
| 界优先 / 尾部保真 / 成本归一（架构名，未获批准） | 先说清内容，命名另议 |

注：论文英文里已有的 `bridge`（parameter bridge、SEI-kinetics bridge 等 7 处）**暂留**，不在中文里推广这个词。

**已清除的比喻词（不要再出现）**

| 禁用 | 改用 |
|---|---|
| 契约 / contract | 设计目标 / design target |
| 协议 / protocol | 流程 / workflow |
| 治理 / governance | 删去，或写清具体指哪条规则 |
| 腿 / leg | run（一次运行） |
| 梯级 / rung | level（层级） |
| 尺子 / yardstick | measurement（测量） |
| 外部厂商 / vendor | 其他模型 / other models |
| 机械判定 / mechanized adjudication | 按规则核对 / code-computed verdict |
| 杠杆 / lever | 设计变量 / design variable |

### 二、不要过度修辞

- 不为强调而造说法（"这不是 A，而是 B"式的自我包装）
- 不把管道 / 工作流说成贡献
- 结论句用数字和事实，不用形容词
- 不用咨询 / 管理学的腔调（"赋能""闭环""抓手""对齐"之类）

### 三、转述不等于续写

描述论文 / 实验结果时，**只写材料里有的**。要补充或推论时，明确标出这是补充；不确定就先问。
（已有教训：幻灯里出现过论文里根本没有的说法，被用户逐页抓出。）

### 四、批量替换规程

单词级机械替换**必然**产生坏句（本项目已两次：节标题被拼成 `…the The design workflow's Rules`）。规程：

1. 先数一遍出现次数与搭配，报给用户再动手
2. 短语级替换，最长优先；保护 `\label` / `\ref` / `\includegraphics` 等内部键
3. 跑完 `grep` 残留 → **逐条读有嫌疑的句子** → 手工修
4. 脚本放 `C:/c1_iso_admin/`，不进仓库

## 论文（2026-09-11 重构要求，逐条执行）

### 定位
1. **完全偏向 AI 与电池**——不要管理学 / 治理学的味道
2. 创新点做两类：**方法/架构型** + **领域应用型**
3. 要**算科研**，不是"把流程跑通了"

### 架构
4. **成体系**（ReAct 级）：一个能命名、别人能照着实现的架构，不是一串并列技巧
5. **与电池设计强相关**：架构的形状由电池的特点决定
6. **带可形式化的理论性**：借数学 / 物理 / 算法的理论稍加改造

### 做法边界
7. **不需要基于现有的 skill**
8. **不要过度包装现有 skill 里的简单机制**（那些机制本身不成体系）
9. **只要骨架在、还能跑通就行**，实现变动多大都可以
10. **不要在现有 skill 和实验里打转**——头脑风暴的对象是架构本身
11. **真正头脑风暴**，不要只用用户举的例子
12. **不要为了形式化而形式化**

### 实验与评测
13. 主实验一律用**省钱的 `official-deepseek-v4-flash`**，API/key 不变
14. **用真实基线数据**
15. **目标不是达到指标值，而是在这些指标上尽量优化、达到更好的性能**
16. 由此可用**公开数据集**评测，与其他论文对比
17. 析锂 / 温度 / 热失控**保留为硬约束**（可行性约束，不是可权衡目标）
18. 主模型换 flash **不需重跑**（8/8 已有）；叙事改为"**同等预算下的最优目标值**"

### 既有约束
- 数值只来自工具输出 / 账本，可逐条重放
- 判定在 agent 之外；判据落盘后不改
- 真计算只签字、不搜索
- 不出现投稿声明（刊名未定）
